git clone 克隆仓库到本地
git branch 新建分支；查看分支
git checkout 切换分支；清除所有操作
git pull 拉取仓库最新改动并merge到本地
git add 将本地修改提交到暂存区
git commit 将本地修改提交
git push origin branch-name 将commit内容提交到远程

git status 查看状态
git diff 查看本地与master区别

git log 查看日志

[alias]
	s = status
	br = branch
	ck = checkout
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cD) %C(bold blue)<%an>%Creset' --abbrev-commit --
        llg = log --oneline --decorate --all --graph --stat

keep updating...
